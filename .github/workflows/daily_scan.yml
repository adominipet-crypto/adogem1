# adogem1.py の一番上の行にこれを追記
import sys
import traceback

def exception_handler(type, value, tb):
    print("★致命的なエラーが発生しました！")
    traceback.print_exception(type, value, tb)

sys.excepthook = exception_handler

# (これより下に、これまでのプログラムを記述)
