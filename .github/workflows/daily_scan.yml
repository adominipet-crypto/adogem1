- name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # 必要なライブラリを網羅的にインストール
          pip install pandas gspread google-auth google-auth-oauthlib requests yfinance
